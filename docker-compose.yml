version: '3'
services:
  python:
    build: 
      context: .
      dockerfile: ./predictor_app_python/Dockerfile
    volumes:
      - ./predictor_app_python:/app
    container_name: python
    stdin_open: true 
    tty: true

  cplus:
    build: 
      context: .
      dockerfile: ./predictor_app_cplus/Dockerfile
    volumes:
      - ./predictor_app_cplus:/app
    container_name: cplus
    stdin_open: true 
    tty: true